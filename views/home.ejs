<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>MediCART | Bangladesh’s Trusted E-Pharmacy</title>

  <!-- ✅ Fix relative path issue -->
  <base href="/" />

  <link rel="stylesheet" href="static/css/home.css" />
</head>
<body>
  <header>
    <div class="navbar">
      <h1>MediCART</h1>
      <nav>
        <a href="/">Home</a>
        <a href="/shop">Shop</a>
        <a href="/cart" id="cart-link">
          🛒 Cart (<span id="cart-count"><%= (session && session.cart ? session.cart.length : 0) %></span>)
        </a>
        <a href="/admin/login">Admin Login</a>
      </nav>
    </div>
  </header>

  <section class="hero">
    <div class="hero-content">
      <h2>Your Health, Our Priority</h2>
      <p>Order trusted medicines and healthcare products online — fast, safe, and delivered across Bangladesh 🇧🇩.</p>
      <a href="/shop" class="btn">🛒 Shop Now</a>
    </div>
  </section>

  <section class="featured">
    <h2>Featured OTC Medicines</h2>
    <div class="product-grid">
      <% if (products && products.length > 0) { %>
        <% products.slice(0,8).forEach(product => { %>
          <div class="product-card">
            <img src="<%= product.imageUrl %>" alt="<%= product.name %>" />
            <h3><%= product.name %></h3>
            <p><%= product.description %></p>
            <span>৳<%= product.price %></span>
            <div class="quantity-section">
              <input type="number" min="1" value="1" class="qty-input" />
              <button class="add-to-cart" data-id="<%= product._id %>">Add to Cart</button>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <p>No products available.</p>
      <% } %>
    </div>
  </section>

  <footer>
    <p>© 2025 MediCART | Developed by Tasfia Afrin Mim</p>
  </footer>

  <script src="static/js/cart.js"></script>
</body>
</html>
