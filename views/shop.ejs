<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Shop | MediCART</title>

  <!-- ✅ Ensure all static paths resolve properly -->
  <base href="/" />

  <link rel="stylesheet" href="static/css/shop.css" />
</head>
<body>
  <header>
    <div class="navbar">
      <h1>MediCART</h1>
      <nav>
        <a href="/">Home</a>
        <a href="/shop">Shop</a>
        <a href="/cart" id="cart-link">
          🛒 Cart (<span id="cart-count"><%= (session && session.cart ? session.cart.length : 0) %></span>)
        </a>
        <a href="/admin/login">Admin Login</a>
      </nav>
    </div>
  </header>

  <section class="shop">
    <h2>All Medicines & Products</h2>
    <div class="product-grid">
      <% if (products && products.length > 0) { %>
        <% products.forEach(product => { %>
          <div class="product-card">
            <img src="<%= product.imageUrl %>" alt="<%= product.name %>" />
            <h3><%= product.name %></h3>
            <p><%= product.description %></p>
            <span>৳<%= product.price %></span>
            <div class="quantity-section">
              <input type="number" min="1" value="1" class="qty-input" />
              <button class="add-to-cart" data-id="<%= product._id %>">Add to Cart</button>
            </div>
          </div>
        <% }) %>
      <% } else { %>
        <p>No products available.</p>
      <% } %>
    </div>
  </section>

  <footer>
    <p>© 2025 MediCART | Developed by Tasfia Afrin Mim</p>
  </footer>

  <script src="static/js/cart.js"></script>
</body>
</html>
